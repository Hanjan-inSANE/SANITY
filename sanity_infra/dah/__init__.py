"""sanity_infra.dah — 0. DAH Interface 러너(예선 로컬).

TM(1, threat_modeler)을 실행해 AttackTree(DM-2)를 JSON으로 산출하고, SM(2)과의 유일 결합점인
sanity:tree:inbox 스트림으로 발행한다(§00-2.2). TM의 LLM도 Gateway 경유(가상키).
"""

__version__ = "0.1.0"
